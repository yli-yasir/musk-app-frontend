import SiteInspectionForm from ".";

export default {
  title: "Components/SiteInspectionForm",
  component: SiteInspectionForm,
};

const Template = (args) => <SiteInspectionForm {...args} />;
export const Primary = Template.bind();
