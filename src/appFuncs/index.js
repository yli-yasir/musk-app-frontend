// login takes a username and password
// returns a payload containing user relevant data OR throws an error with an appropriate failure message.
// failure can occur due to a general server error, or invalid credentials.

export { default } from "./appFuncsMock";
